<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="10dp">

    <TextView
        android:id="@+id/txtRescanHint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="SWIPE UP to rescan the network for any new devices"
        android:textAlignment="center"
        android:textColor="#B0B0B0"
        android:textSize="12sp"
        android:layout_marginBottom="8dp"/>

    <Button
        android:id="@+id/btnAddDeviceManual"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="+ Add Device Manually"
        android:textColor="@color/flux_neon_blue"
        android:layout_marginBottom="8dp"/>

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshDevices"
        android:layout_width="match_parent"
        android:layout_height="450dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerDeviceList"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="4dp"
            android:clipToPadding="false"/>

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

</LinearLayout>